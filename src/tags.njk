---
layout: base.njk
title: Tags
permalink: "/tags/"
---

<h1>Tags</h1>

{% if collections.tagged and (collections.tagged | keys | length) %}
  <ul class="tag-list">
    {% for tag in collections.tagged | keys | sort %}
      {% set tagSlug = tag | slug %}
      {% set items = collections.tagged[tag] %}
      <li>
        <a href="{{ ('/tags/' + tagSlug + '/') | url }}">{{ tag }}</a>
        <small>({{ items | length }})</small>
      </li>
    {% endfor %}
  </ul>
{% else %}
  <p>No tags found.</p>
{% endif %}







