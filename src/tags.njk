---
layout: base.njk
title: Tags
---

<h1>Tags</h1>
<ul>
{% for tag in collections.tagged | keys | sort %}
  <li>
    <a href="{{ '/tags/' ~ (tag | slug) ~ '/' | url }}">{{ tag }}</a>
    ({{ collections.tagged[tag].length }})
  </li>
{% endfor %}
</ul>


